# Grafana datasource — TimescaleDB (PostgreSQL)
# TODO Phase 3 (Day 29)
apiVersion: 1
datasources:
  - name: TimescaleDB
    type: postgres
    url: timescaledb:5432
    database: alphadesk
    user: alphadesk
    secureJsonData:
      password: password
    jsonData:
      sslmode: disable
      timescaledb: true
